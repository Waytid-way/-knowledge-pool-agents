name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:0.8.5-pg16-bookworm
        env:
          POSTGRES_USER: llm_wiki
          POSTGRES_PASSWORD: llm_wiki
          POSTGRES_DB: llm_wiki
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U llm_wiki -d llm_wiki"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      LLM_WIKI_DATABASE_URL: postgresql+psycopg://llm_wiki:llm_wiki@localhost:5432/llm_wiki
      LLM_WIKI_TEST_DATABASE_URL: postgresql+psycopg://llm_wiki:llm_wiki@localhost:5432/llm_wiki
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
          enable-cache: true
      - run: uv sync --all-extras
      - name: Render frozen PostgreSQL schema
        run: |
          uv run python - <<'PY' > frozen-schema.sql
          from sqlalchemy.dialects import postgresql
          from sqlalchemy.schema import CreateTable
          from llm_wiki.db import models as _models  # noqa: F401
          from llm_wiki.db.base import Base

          dialect = postgresql.dialect()
          for table in Base.metadata.sorted_tables:
              print(str(CreateTable(table).compile(dialect=dialect)).strip())
              print(";")
          PY
      - name: Upload frozen PostgreSQL schema
        uses: actions/upload-artifact@v4
        with:
          name: task3-frozen-schema
          path: frozen-schema.sql
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run mypy src
      - name: Capture Alembic diagnostics
        run: uv run alembic upgrade head > alembic-diagnostics.txt 2>&1 || true
      - name: Upload Alembic diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: task3-conflict-alembic
          path: alembic-diagnostics.txt
      - run: uv run alembic upgrade head
      - run: uv run pytest -q
      - run: uv run alembic downgrade base
      - run: uv run alembic upgrade head
