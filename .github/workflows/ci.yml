name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
          enable-cache: true
      - run: uv sync --all-extras
      - run: uv run ruff check .
      - name: Capture Ruff format diagnostics
        run: uv run ruff format --check . > ruff-format-diagnostics.txt 2>&1 || true
      - name: Upload Ruff format diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: ruff-format-diagnostics
          path: ruff-format-diagnostics.txt
      - run: uv run ruff format --check .
      - run: uv run mypy src
      - run: uv run pytest -q
